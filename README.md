# setup

make sure to create a database named "attendance"
and import the sql file in the root folder

# backend
```bash
cd api
npm install
node index.js
```

# frontend
```bash
cd frontend
npm install
npm run serve
